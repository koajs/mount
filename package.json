{
  "name": "koa-mount",
  "description": "Mounting middleware for koa",
  "repository": "koajs/mount",
  "version": "1.3.0",
  "keywords": [
    "koa",
    "middleware",
    "mount",
    "mounting"
  ],
  "files": [
    "index.js"
  ],
  "devDependencies": {
    "istanbul-harmony": "0",
    "koa": "latest",
    "should": "^3.0.0",
    "mocha": "^1.12.1",
    "supertest": "0"
  },
  "license": "MIT",
  "dependencies": {
    "koa-compose": "^2.2.0",
    "debug": "*"
  },
  "scripts": {
    "test": "NODE_ENV=test mocha --require should --reporter spec",
    "test-cov": "NODE_ENV=test istanbul cover ./node_modules/.bin/_mocha -- --require should",
    "test-travis": "NODE_ENV=test istanbul cover ./node_modules/.bin/_mocha --report lcovonly -- --require should"
  }
}

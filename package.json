{
  "name": "koa-mount",
  "description": "Mounting middleware for Koa",
  "repository": "koajs/mount",
  "version": "4.2.0",
  "keywords": [
    "koa",
    "middleware",
    "mount",
    "mounting"
  ],
  "files": [
    "index.js"
  ],
  "dependencies": {
    "koa-compose": "^4.1.0"
  },
  "devDependencies": {
    "c8": "^12.0.0",
    "eslint-config-xo-lass": "^2.0.1",
    "husky": "^9.1.7",
    "koa": "^3.2.1",
    "koa-static": "^5.0.0",
    "supertest": "^7.2.2",
    "xo": "^0.60.0"
  },
  "peerDependencies": {
    "koa": ">= 2"
  },
  "license": "MIT",
  "scripts": {
    "lint": "xo",
    "lint:fix": "xo --fix",
    "pretest": "npm run lint",
    "test": "node --test test/*.test.js --test-force-exit",
    "test-ci": "c8 --reporter=lcov npm run test",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
